(Probe X Axis from RIGHT into a block and calculate offset)
<pre>
o108 sub
( Set current X position to 0 so that we will always be moving right )
G10 L20 P0 X0

( into block )
G38.2 X-1 f10

G10 L20 P0 X+.125
( Rapid away )
G0 X+1.371
o108 endsub
</pre>
